import React from 'react';
import { render, cleanup } from '@testing-library/react';

import SocialLinksWidgetWrapper from '../ts/components/widgets/SocialLinksWidgetWrapper';

describe('Tests for SocialLinksWidget', () => {
	it('should render to dom', () => {
		const {  }
	});
});
